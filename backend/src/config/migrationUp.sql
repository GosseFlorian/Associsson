CREATE TYPE statut AS ENUM (
	'a_faire',
	'en_cours',
	'termine'
);

CREATE TYPE priorite AS ENUM (
	'faible',
	'moyenne',
	'haute',
	'tres_haute'
);

CREATE TYPE role AS ENUM (
	'admin',
	'benevole',
	'licencie'
);

CREATE TABLE IF NOT EXISTS utilisateur (
	id INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	nom VARCHAR(255) NOT NULL,
	email VARCHAR(255) NOT NULL UNIQUE,
	mot_de_passe VARCHAR(255) NOT NULL,
	date_inscription TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
	PRIMARY KEY(id)
);

CREATE TABLE IF NOT EXISTS membre (
	id INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	organisation_id INTEGER NOT NULL,
	utilisateur_id INTEGER NOT NULL,
	role role NOT NULL,
	PRIMARY KEY(id)
);

CREATE TABLE IF NOT EXISTS organisation (
	id INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	nom VARCHAR(255) NOT NULL,
	date_creation TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
	est_actif BOOLEAN NOT NULL DEFAULT false,
	proprietaire_id INTEGER NOT NULL,
	PRIMARY KEY(id)
);

CREATE TABLE IF NOT EXISTS projet (
	id INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	organisation_id INTEGER NOT NULL,
	createur_id INTEGER NOT NULL,
	titre VARCHAR(255) NOT NULL,
	description VARCHAR(255),
	date_creation TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
	date_debut TIMESTAMPTZ,
	date_fin TIMESTAMPTZ,
	adresse VARCHAR(255),
	est_termine BOOLEAN NOT NULL DEFAULT false,
	PRIMARY KEY(id)
);

CREATE TABLE IF NOT EXISTS tache (
	id INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	projet_id INTEGER NOT NULL,
	titre VARCHAR(255) NOT NULL,
	description VARCHAR(255),
	statut statut NOT NULL,
	priorite priorite NOT NULL,
	date_echeance TIMESTAMPTZ,
	assigne_a INTEGER,
	PRIMARY KEY(id)
);

ALTER TABLE membre
ADD FOREIGN KEY(utilisateur_id) REFERENCES utilisateur(id)
ON UPDATE CASCADE ON DELETE CASCADE;

ALTER TABLE membre
ADD FOREIGN KEY(organisation_id) REFERENCES organisation(id)
ON UPDATE CASCADE ON DELETE CASCADE;

ALTER TABLE tache
ADD FOREIGN KEY(projet_id) REFERENCES projet(id)
ON UPDATE CASCADE ON DELETE CASCADE;

ALTER TABLE organisation
ADD FOREIGN KEY(proprietaire_id) REFERENCES utilisateur(id)
ON UPDATE CASCADE ON DELETE CASCADE;

ALTER TABLE tache
ADD FOREIGN KEY(assigne_a) REFERENCES membre(id)
ON UPDATE CASCADE ON DELETE SET NULL;

ALTER TABLE projet
ADD FOREIGN KEY(createur_id) REFERENCES membre(id)
ON UPDATE CASCADE ON DELETE CASCADE;

ALTER TABLE projet DROP CONSTRAINT fk_projet_organisation;
ALTER TABLE projet
ADD FOREIGN KEY(organisation_id) REFERENCES organisation(id)
ON UPDATE CASCADE ON DELETE CASCADE;